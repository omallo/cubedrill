// Cubedrill is a local-first, client-rendered app: cube rendering (cubing.js),
// smart-cube Bluetooth, and browser-storage persistence are all client-only.
// Disabling SSR keeps a single, simple rendering model across every view.
export const ssr = false;
